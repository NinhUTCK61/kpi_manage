export enum ViewPortAction {
  Move = 'move',
  Pan = 'pan',
  Comment = 'comment',
  SpeechBallon = 'speechBallon',
}

export enum StyleText {
  Bold = 'bold',
  Italic = 'italic',
}

export enum TextAlign {
  Left = 'left',
  Center = 'center',
  Right = 'right',
  Unset = 'unset',
}
