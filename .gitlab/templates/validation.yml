.required-variable:
  before_script:
    - |
      if [ -z "$APP_NAME" ]; then
        echo "APP_NAME is not set"
        exit 1
      fi
    - |
      if [ -z "$PORT" ]; then
        echo "PORT is not set"
        exit 1
      fi